spring:
  datasource:
    url: jdbc:mariadb://127.0.0.1:3306/maskshop?characterEncoding=utf8
    username: maskshop
    password: maskshop
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true


logging.level:
  org.hibernate.SQL: debug



